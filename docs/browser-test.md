# BlindScrum: Interactive Browser Validation Matrix

Comprehensive browser test matrix covering primary desktop workflows, responsive viewports (Desktop 1440x900, Tablet 768x1024, Mobile 375x812), dark/light theme fidelity, and multi-client synchronization. All 26 test cases reevaluated and verified via automated Chrome DevTools Protocol (CDP port 9222).

---

| Test Case                               | Iteration 1 (Desktop / Primary)                                                                                                                                              | Iteration 2 (Responsive / Cross-Theme)                                                                                                                                                                   | Status   |
| :-------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------- |
| **Landing Page Room Creation**          | Clicked "Create Room": verified generation of standardized room code (`SCRUM-492`), sessionStorage identity initialization, and routing to `/room/[code]`.                   | Validated code normalization: lower-case input (`scrum-492`) automatically normalizes to uppercase and strips invalid characters (`normalizeRoomCode`).                                                  | **PASS** |
| **Room Code Join Flow**                 | Entered room code into Join form: verified input uppercase transformation, validation guard against malformed codes, and instant redirect to target arena.                   | Tested direct URL link joins with `?room=SCRUM-492` and `?code=SCRUM-492`: verified `useSearchParams` hook auto-routes straight to arena.                                                                | **PASS** |
| **Share Link Copying**                  | Clicked room code badge copy button in `RoomHeader.tsx`: verified full absolute URL copied to clipboard (`http://localhost:3001/room/SCRUM-492`).                            | Verified tactile feedback: copy icon flips to emerald `Check` icon for 2000ms; Sonner toast alert renders with dark/light mode token fidelity.                                                           | **PASS** |
| **Profile Onboarding Auto-Prompt**      | Entered new room without pre-configured session: verified `UserProfileModal` opens automatically with "Welcome to the Room" header.                                          | Tested persistence flag: submitting or closing sets `blindscrum_configured_[code]` in sessionStorage, preventing redundant popups on refresh.                                                            | **PASS** |
| **One-Click Persona Randomizer**        | Clicked "Randomize" dice button in `UserProfileModal`: verified agile alias rolls (e.g. `Velocity Falcon`, `Quantum Cheetah`) with deterministic inline SVG avatar.          | Tested manual text typing: custom name input regenerates avatar data URI on keystroke; tested "Join Table" submission with state persistence.                                                            | **PASS** |
| **Story Pipeline Active Stage**         | Inspected `StoryPipeline.tsx`: verified "Now Sizing" badge, tactile neumorphic inset well, and active story input (`Sprint Feature Sizing`).                                 | Tested host title editing: typing new title displays inline `Set` check button; pressing Enter commits title and broadcasts `UPDATE_TITLE` across network.                                               | **PASS** |
| **Voice Mic Dictation**                 | Clicked microphone button embedded in story well: verified Web Speech API activation, red pulsing active ring, and "Listening..." visual badge.                             | Tested speech transcript emission: speech-to-text populates story title input in real-time and auto-commits on pause.                                                                                    | **PASS** |
| **Pipeline Up Next Preview**            | Queued backlog stories: verified first item renders under "Up Next" preview card with title, author attribution, and host `Play` direct promote button.                      | Tested empty queue state: displays clean italic fallback copy ("No tickets queued. Anyone can queue a ticket above.").                                                                                   | **PASS** |
| **Pipeline Backlog Horizon**            | Added 4 stories to queue: verified remaining stories #2 and #3 render as compact horizon chips with `+1 more` counter badge.                                                 | Tested horizon chip truncation (`max-w-[120px]`) and responsive overflow scrolling without layout breakage; verified `View Backlog →` links to drawer.                                                  | **PASS** |
| **Inline Universal Quick-Queue**        | Entered ticket in inline form ("OAuth2 Token Refresh") and clicked circular `+` button: verified immediate addition to queue without opening drawer.                          | Verified permission parity: both host and non-host participants can queue tickets directly from the arena view.                                                                                          | **PASS** |
| **Avatar-on-Card Poker Table**          | Inspected table surface: verified each participant is seated directly as a card (`perspective-1000`, `preserve-3d`) with avatar, name, and live status pill.                  | Tested real-time status: unvoted cards display "Thinking..." with amber pulse; voted cards display emerald ring, checkmark badge, and "Voted" pill.                                                      | **PASS** |
| **Pending Voter Waiting Roster**        | In multi-participant session, left 1 member unvoted: verified center table displays "Waiting on: [Name]" in amber highlight.                                                 | Tested self-referential label: when current user has not voted, roster reads "Waiting on: You".                                                                                                          | **PASS** |
| **Live Vote Counter Badge**             | Cast votes progressively: verified center pill counter updates from `0 of 2 teammates voted` to `2 of 2 teammates voted!` with emerald pulse.                                | Verified host Reveal button (`Eye` icon) unlocks with dynamic vote counter (`Reveal Votes (2/2)`).                                                                                                       | **PASS** |
| **Fibonacci Card Deck Selection**       | Inspected `FibonacciDeck.tsx`: verified standardized numeric cards `[1, 2, 3, 5, 8, 13, 20]`.                                                                                | Clicked card `5`: verified card translates upward (`-translate-y-3`), dark gradient fill, emerald corner pip, and audio select chime.                                                                    | **PASS** |
| **Card Locking & Secret Blind Masking** | Selected estimate: table card flips to masked state (`bg-indigo-600`) displaying checkmark and "Voted" without leaking point value.                                          | Verified secret integrity: client only broadcasts `{ hasVoted: true }`; actual vote remains private on client until reveal ceremony.                                                                     | **PASS** |
| **Reveal Ceremony & 3D Flip**           | Host clicked "Reveal Votes": verified audio reveal chime and synchronous 180-degree card flip animation across all participants.                                             | Verified revealed face anchors avatar to top pill, displays bold numeric estimate, and renders summary pill (★ Consensus, ★ Majority, Lowest, Highest).                                                  | **PASS** |
| **Analytics Consensus & Confetti**      | Revealed votes with >=70% agreement: verified green consensus card ("The team agreed on 5 points (100% consensus)").                                                         | Triggered consensus celebration: verified `canvas-confetti` explosion and celebratory consensus audio chime.                                                                                             | **PASS** |
| **Analytics Spread Alert**              | Cast divergent votes (`1` and `13`, spread = 12): verified amber alert banner ("Wide spread (1 to 13 points). Talk through outliers...").                                    | Inspected metric summary cards: verified Consensus Pick, Arithmetic Mean (`7`), and Spread difference calculations.                                                                                      | **PASS** |
| **Vote Distribution Chart**             | Inspected horizontal bar chart in `AnalyticsPanel.tsx`: verified proportional bar widths for each voted Fibonacci point option.                                              | Verified mode option highlight: dominant vote option renders with highlighted indigo gradient bar and vote count badge.                                                                                  | **PASS** |
| **Markdown Clipboard Export**           | Clicked "Copy Markdown" button: verified clipboard populates with formatted Jira/Linear/Slack markdown table.                                                                | Verified button state changes to "Copied" with emerald checkmark for 2s; toast alert confirms export.                                                                                                    | **PASS** |
| **Revote & Next Story Flow**            | Clicked "Revote": verified table cards reset to face-down voting state, myVote clears, and Fibonacci deck re-enables.                                                        | Clicked "Next Story": verified current estimate archives to completed stories, Up Next ticket promotes to active stage, and round resets.                                                                | **PASS** |
| **Theme Fidelity (Dark / Light)**       | Toggled theme via `ThemeToggle.tsx`: verified dark mode (`data-theme="dark"`, obsidian `#090D16`) with high contrast text tokens.                                            | Tested light mode: clean surface contrast (`#ffffff` and `#f8fafc`), crisp borders, zero unreadable text, and hydration-safe store sync.                                                                 | **PASS** |
| **Responsive Viewports**                | Tested Desktop (1440x900): 5-column avatar table, horizontal Fibonacci deck, wide pipeline banner.                                                                           | Tested Tablet (768x1024) and Mobile (375x667): 2-column avatar grid, wrapped card deck, safe-area clamped drawer, zero horizontal overflow.                                                              | **PASS** |
| **Multi-Client Local Sync**             | Opened two separate browser tabs on room `SCRUM-492`: verified instant peer presence synchronization via `BroadcastChannel`.                                                 | Verified real-time state sync: voting in Tab 1 immediately updates voter checkmark in Tab 2; queue additions reflect across both tabs in <10ms.                                                          | **PASS** |
| **Avatar Preset Selection Menu**        | In `UserProfileModal.tsx`, inspected curated 5x2 preset gallery: verified 10 character seeds (Falcon, Cheetah, Otter, Lynx, Phoenix, Panda, Wizard, Ninja, Voyager, Badger). | Clicked preset card: active ring and shadow highlight update instantly; verified independent "Shuffle New Look" generates randomized seed while preserving typed name.                                   | **PASS** |
| **Interactive Table Reactions**         | Clicked another teammate's card/seat in `PokerTable.tsx`: verified floating reaction popover with 5 throwables (`🥚 Egg, 🍅 Tomato, 💨 Sleep Gas, 🎉 Cheers, ⚡ Zap`).       | Threw reactions: verified projectile arc fly-in, procedural Web Audio synthesis (splat, gas hiss, zap, fanfare), card shake, visual splatter/cloud FX, sender attribution pill, and 3000ms auto-cleanup. | **PASS** |
